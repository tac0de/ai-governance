{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-governance/schemas/jsonschema/result.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "request_id", "actions"],
  "properties": {
    "version": { "type": "string", "enum": ["1.0", "1.1"] },
    "request_id": { "type": "string", "format": "uuid" },
    "determinism_id": { "type": "string", "format": "uuid" },
    "git_commit": { "type": "string", "pattern": "^[a-f0-9]{40}$" },
    "pr_ref": { "type": "string", "minLength": 1 },
    "actions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "artifacts", "stdout_summary", "stderr_summary"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "status": { "type": "string", "enum": ["ok", "error"] },
          "artifacts": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["path", "sha256"],
              "properties": {
                "path": { "type": "string", "minLength": 1 },
                "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
              }
            }
          },
          "stdout_summary": { "type": "string" },
          "stderr_summary": { "type": "string" }
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "version": { "const": "1.1" }
        },
        "required": ["version"]
      },
      "then": {
        "required": ["determinism_id", "git_commit", "pr_ref"]
      }
    }
  ]
}
