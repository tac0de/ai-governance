# Control Plane Guard Negative Test

This file intentionally lives under `apps/` to verify that the
`Constitution Scope Check` workflow blocks non-control-plane path changes
in `main-hub` pull requests.
