# universal (alias for codex)

This folder is an alias wrapper for the original `codex` runner directory.

- Original location: `codex/`
- Use `../codex/runner/codex.mjs` to run existing CLI behavior.

This wrapper preserves compatibility while the repository transitions to the universal LLM connector naming.
