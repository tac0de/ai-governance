# universal-engine (alias for codex-engine)

This folder is an alias wrapper for the original `codex-engine` directory.

- Original location: `codex-engine/`

It preserves references so tooling or documentation can point to `universal-engine` during migration.
