# MCP Implementations Location

This directory does not contain executable MCP implementations.

Boundary rule:
- `ai-governance` keeps governance contracts and conformance fixtures only.
- Real MCP implementations are managed in external sibling repositories (for example `../mcp-remote-*`, `../mcp-local-*`).
- Any implementation code added here is out of policy and must be rejected.
