# Capabilities

- `assign_cohort`: deterministic cohort mapping from stable actor key.
- `resolve_flag`: deterministic feature flag resolution by rollout contract.
- `kill_switch`: immediate disable path with trace reason.
