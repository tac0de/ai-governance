name: "governance: release gate"

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      approver_role:
        description: "Operating profile release role (OPM-V1): ControlPlaneOperator"
        required: false
        default: ControlPlaneOperator
        type: string
      secret_tier:
        description: "Operating profile secret tier (OPM-V1): none | admin-only | repo-write"
        required: false
        default: admin-only
        type: string

permissions: read-all

jobs:
  release-gate:
    runs-on: ubuntu-latest
    env:
      APPROVER_ROLE: ${{ github.event.inputs.approver_role != '' && github.event.inputs.approver_role || vars.RELEASE_APPROVER_ROLE }}
      SECRET_TIER_INPUT: ${{ github.event.inputs.secret_tier }}
      SECRET_TIER_REPO_VAR: ${{ vars.SECRET_TIER }}
      OPENAI_ADMIN_API_KEY: ${{ secrets.OPENAI_ADMIN_API_KEY }}
      REPO_ACCESS_TOKEN: ${{ secrets.REPO_ACCESS_TOKEN }}
      ROLES_CHARTER_VERSION: v1
      OPM_VERSION: v1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enforce secrets tier gate
        run: |
          set -euo pipefail
          tier="${SECRET_TIER_INPUT:-${SECRET_TIER_REPO_VAR:-admin-only}}"
          if [ -z "$tier" ]; then
            tier="admin-only"
          fi
          case "$tier" in
            none)
              ;;
            admin-only)
              if [ -z "${OPENAI_ADMIN_API_KEY:-}" ]; then
                echo "Missing required secret for tier=admin-only: OPENAI_ADMIN_API_KEY"
                exit 1
              fi
              ;;
            repo-write)
              if [ -z "${OPENAI_ADMIN_API_KEY:-}" ] || [ -z "${REPO_ACCESS_TOKEN:-}" ]; then
                echo "Missing required secrets for tier=repo-write: OPENAI_ADMIN_API_KEY, REPO_ACCESS_TOKEN"
                exit 1
              fi
              ;;
            *)
              echo "Invalid SECRET_TIER: $tier"
              exit 1
              ;;
          esac
          echo "SECRET_TIER=$tier" >> "$GITHUB_ENV"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.x"
          cache-dependency-path: control-plane/go/go.sum

      - name: Build control-plane runtime
        working-directory: control-plane/go
        run: |
          set -euo pipefail
          go build -o /tmp/cp-bin ./cmd/hub

      - name: Run deterministic gate path
        run: |
          set -euo pipefail
          mkdir -p out/release-gate
          /tmp/cp-bin validate --plan constitution/cases/pass/case001.plan.json
          /tmp/cp-bin run --plan constitution/cases/pass/case001.plan.json --outdir out/release-gate
          /tmp/cp-bin audit \
            --plan constitution/cases/pass/case001.plan.json \
            --result out/release-gate/result.json \
            --evidence out/release-gate/evidence.json \
            --out out/release-gate/audit.json

      - name: Verify audit verdict
        run: |
          set -euo pipefail
          status=$(jq -r '.status' out/release-gate/audit.json)
          if [ "$status" != "PASS" ]; then
            echo "Release blocked: audit status=$status"
            exit 1
          fi

      - name: Verify release authority role
        run: |
          set -euo pipefail
          role="${APPROVER_ROLE:-}"
          if [ "$role" != "ControlPlaneOperator" ]; then
            echo "Release blocked: approver role must be ControlPlaneOperator (got: ${role:-<empty>})"
            exit 1
          fi

      - name: Verify roles charter version tag
        run: |
          set -euo pipefail
          if [ "${ROLES_CHARTER_VERSION:-}" != "v1" ]; then
            echo "Release blocked: ROLES_CHARTER_VERSION must be v1"
            exit 1
          fi
          if ! grep -q "ROLES_CHARTER_VERSION=v1" constitution/charter/AI_GOVERNANCE_ROLES_V1.md; then
            echo "Release blocked: charter tag mismatch"
            exit 1
          fi

      - name: Verify operating profile version tag
        run: |
          set -euo pipefail
          if [ "${OPM_VERSION:-}" != "v1" ]; then
            echo "Release blocked: OPM_VERSION must be v1"
            exit 1
          fi
          if ! grep -q "OPM_VERSION=v1" constitution/policies/OPERATING_PROFILE_MATRIX_V1.md; then
            echo "Release blocked: operating profile tag mismatch"
            exit 1
          fi

      - name: Log operating profile version
        run: |
          set -euo pipefail
          echo "Operating profile version: ${OPM_VERSION}"


      - name: Write release status artifact
        if: always()
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import datetime
          import hashlib
          import json
          import pathlib
          import os

          base = pathlib.Path('out/release-gate')
          base.mkdir(parents=True, exist_ok=True)

          audit_path = base / 'audit.json'
          if audit_path.exists():
            audit = json.loads(audit_path.read_text())
            status = audit.get('status')
            reason_code = audit.get('reason_code')
            request_id = audit.get('request_id')
            checksums = audit.get('checksums', {})
          else:
            status = 'ERROR'
            reason_code = None
            request_id = None
            checksums = {}

          track_state = 'DONE' if (status == 'PASS' and reason_code is None) else 'OPEN'

          summary = {
            'checked_at': datetime.datetime.utcnow().replace(microsecond=0).isoformat() + 'Z',
            'status': status,
            'reason_code': reason_code,
            'track_state': track_state,
            'request_id': request_id,
            'checksums': checksums,
            'mode': 'silent-signal-only',
            'opm_version': os.environ.get('OPM_VERSION', 'unknown'),
            'incident_hint': {
              'template': 'constitution/decision-log/INC-0001-template.md',
              'required_fields': [
                'workflow',
                'run_url',
                'reason_code',
                'root_cause',
                'fix_commit'
              ]
            }
          }

          payload = json.dumps(summary, sort_keys=True).encode('utf-8')
          summary['summary_hash'] = hashlib.sha256(payload).hexdigest()

          (base / 'status.json').write_text(json.dumps(summary, indent=2) + \"\\n\")
          PY

      - name: Append release summary
        if: always()
        run: |
          set -euo pipefail
          status=$(python3 - <<'PY'
          import json
          print(json.load(open('out/release-gate/status.json'))['status'])
          PY
          )
          reason=$(python3 - <<'PY'
          import json
          v = json.load(open('out/release-gate/status.json')).get('reason_code')
          print(v if v is not None else 'null')
          PY
          )
          checked=$(python3 - <<'PY'
          import json
          print(json.load(open('out/release-gate/status.json'))['checked_at'])
          PY
          )
          opm=$(python3 - <<'PY'
          import json
          print(json.load(open('out/release-gate/status.json'))['opm_version'])
          PY
          )
          track=$(python3 - <<'PY'
          import json
          print(json.load(open('out/release-gate/status.json'))['track_state'])
          PY
          )
          echo "## Release Gate" >> "$GITHUB_STEP_SUMMARY"
          echo "- checked_at: $checked" >> "$GITHUB_STEP_SUMMARY"
          echo "- secret_tier: ${SECRET_TIER:-unknown}" >> "$GITHUB_STEP_SUMMARY"
          echo "- approver_role: ${APPROVER_ROLE:-unknown}" >> "$GITHUB_STEP_SUMMARY"
          echo "- opm_version: $opm" >> "$GITHUB_STEP_SUMMARY"
          echo "- track_state: $track" >> "$GITHUB_STEP_SUMMARY"
          echo "- status: $status" >> "$GITHUB_STEP_SUMMARY"
          echo "- reason_code: $reason" >> "$GITHUB_STEP_SUMMARY"
          echo "- issue/comment: none (silent mode)" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload release artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-gate-${{ github.run_id }}
          path: |
            out/release-gate/status.json
            out/release-gate/audit.json
            out/release-gate/result.json
            out/release-gate/evidence.json
            out/release-gate/logs/
