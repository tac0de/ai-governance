{
  "version": "v0.1",
  "append_only": true,
  "hash_reference_required": true,
  "allowed_record_types": [
    "jsonl_trace_record",
    "bridge_intent",
    "governance_journal_packet",
    "governance_journal_receipt"
  ],
  "forbidden_mutations": [
    "Record deletion.",
    "In-place record rewrite.",
    "Wall-clock fields in deterministic verdict path.",
    "Randomized verdict inputs.",
    "Network-dependent verdict evaluation."
  ],
  "retention_model": {
    "primary_store": "traces/",
    "export_path": "traces/governance-journal/",
    "notes": [
      "Primary runtime traces remain append-only under traces/.",
      "Long-lived governance diary copies are exported after validation.",
      "Trace packets and receipts stay hash-referenced."
    ]
  },
  "journal_export_boundary": {
    "mcp_id": "core-governance-journal-mcp",
    "target_service": "obsidian-mcp",
    "requirements": [
      "Only validated governance artifacts may be exported.",
      "Export payloads must remain trace-linked and hash-verifiable.",
      "An export receipt artifact is required before completion."
    ]
  }
}
