# Capabilities

- `export_trace_bundle`: exports a validated governance trace bundle for long-term retention.
- `append_governance_diary_entry`: appends a normalized diary entry to the external governance journal.
- `verify_export_receipt`: verifies that the external journal acknowledged receipt of the export.
