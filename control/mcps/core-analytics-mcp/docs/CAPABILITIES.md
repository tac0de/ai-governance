# Capabilities

- `aggregate_events`: deterministic rollup by UTC key and event name.
- `compute_kpi_summary`: produces fixed KPI keys aligned to service SLO contract.
- `detect_anomaly`: threshold-based anomaly detection with explicit policy references.
- `propose_learning_actions`: deterministic action proposal set derived from KPI thresholds.
