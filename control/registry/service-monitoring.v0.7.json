{
  "version": "v0.7",
  "model": "continuous-service-monitoring",
  "purpose": "Define the continuous monitoring rules used to classify the current structural and hygiene state of both new and existing linked services.",
  "applies_to": [
    "new linked service",
    "existing linked service"
  ],
  "required_artifacts": [
    "service.snapshot.json",
    "hygiene.report.json",
    "cleanup_action_ref"
  ],
  "monitoring_status_levels": [
    "pending",
    "active",
    "attention",
    "cleanup-required"
  ],
  "trigger_conditions": [
    "required-kernel-path-missing",
    "required-orchestration-path-missing",
    "required-prompt-path-missing",
    "dtp-validator-stale",
    "duplicate-surface-growth",
    "stale-governance-growth",
    "oversized-runtime-surface",
    "protected-path-risk"
  ],
  "hard_rules": [
    "Central governance may classify service structure and hygiene state but must not execute runtime cleanup.",
    "A cleanup-required monitoring state may block release readiness.",
    "All continuous judgments must be based on service.snapshot.json and hygiene.report.json."
  ]
}
