# Control Agents

Central control can assign virtual departments here.

- `departments.v0.1.json`: department catalog and default prompt-set bindings.
- This is governance metadata only, not runtime staff or model profile storage.
